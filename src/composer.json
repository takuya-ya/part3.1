{
    "name": "root/html",
    "version": "1.0.0",
    "autoload": {
        "psr-4": {
            "Root\\Html\\": "src/"
        }
    },
    "require-dev": {
            "squizlabs/php_codesniffer": "^3.0",
            "phpmd/phpmd" : "@stable",
        "phpstan/phpstan": "^1.12"
        },
    "scripts": {
            "phpcs": "vendor/bin/phpcs --standard=phpcs.xml",
            "phpmd": "vendor/bin/phpmd bread_sales.php text phpmd.xml --suffixes php --exclude vendor",
            "phpstan": "vendor/bin/phpstan analyse bread_sales.php"
    }
}
